import React from 'react';

const Settings = () => {
  return (
    <div className="container">
      <h2>Settings</h2>
      <p>Here you can configure system parameters (temperature limits, CO2 levels, humidity thresholds, etc).</p>
    </div>
  );
};

export default Settings;